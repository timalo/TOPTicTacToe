const gameBoard = () => {
    let board = [];
}

const player = (name) => {
    let points = 0;
    const getName = () => console.log(name);
    return {getName};
};

const gameController = () => {

}